#include <bits/stdc++.h>
#define ll long long
using namespace std;

int main()
{
    int tt;
    cin >> tt;
    while (tt--)
    {
        int nn;
        cin >> nn;
        int xx = ceil(nn / 2.0);
        cout << xx << endl;
    }

    return 0;
}
